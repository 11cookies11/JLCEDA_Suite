interface:
  display_name: "Hardware Development Skill"
  short_description: "Use repo resources to build, validate, and improve hardware designs"
  default_prompt: "Use $kicad-suite-skill to develop the hardware design with the repository's models, configs, symbols, validation tools, and KiCad pipeline."

policy:
  allow_implicit_invocation: true
