interface:
  display_name: "JLCEDA Bridge Flow"
  short_description: "Drive connected JLCEDA sessions from Codex"
  default_prompt: "Use $jlceda-bridge-flow to run the JLCEDA server-side project flow and drive a connected bridge session."

policy:
  allow_implicit_invocation: true
